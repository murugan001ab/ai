// Re-export everything from the central store
export { stats, trendData, violations, alerts, cameras } from '../store'
export type { TrendData, ViolationData, Camera, Stat, Alert } from '../types/dashboard'
